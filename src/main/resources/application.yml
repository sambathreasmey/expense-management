spring:
  application:
    name: expense-service
  datasource:
    url: 'jdbc:mysql://localhost:3307/expense_db'
    username: admin
    password: 123
    driver-class-name: com.mysql.cj.jdbc.NonRegisteringDriver
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
      password: ex123456 # Optional
      database: 0
      timeout: 2000ms

cached:
  category:
    key: CATEGORY
    ttl: 60